{
  "name": "opencode-remote-channel",
  "version": "0.1.0",
  "description": "Remote channel plugin for OpenCode - control OpenCode via HTTP/WebSocket",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "echo \"No tests yet\""
  },
  "keywords": ["opencode", "plugin", "channel", "remote"],
  "license": "MIT",
  "peerDependencies": {
    "opencode": ">=2026.3.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "dependencies": {
    "ws": "^8.14.0",
    "zod": "^3.22.0"
  }
}
